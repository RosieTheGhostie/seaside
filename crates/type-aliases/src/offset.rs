/// A signed offset, usually to a [memory address](crate::Address).
pub type Offset = i32;

/// The unsigned counterpart to [`Offset`].
///
/// Mostly useless, but it can come up sometimes.
pub type UnsignedOffset = u32;
